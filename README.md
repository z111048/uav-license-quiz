# 無人機學科線上練習系統 (UAV License Quiz)

這是一個簡易的網頁版互動式練習題庫，專為準備無人機學科考試設計。除了基本的測驗功能外，還引入了**「無腦背白名單」**邏輯，幫助考生快速識別唯一正確的選項。

## 🚀 功能特色

### 1. 智慧輔助學習
- **無腦背白名單 (Memorization Whitelist)**：
  - 系統會透過演算法分析題庫，篩選出那些**只要看到該選項內容，就一定是正確答案**的選項。
  - 提供獨立的「白名單清單」視圖，方便考生針對這些關鍵字進行強化記憶。
- **智慧標記**：
  - 在「閱讀模式」中，符合白名單規則的題目會標記 **「⚡ 可無腦背」** 標籤，提醒考生該題可以直接背答案。

### 2. 多元作答模式
- **計時測驗**：每題有倒數計時（預設 10 秒），模擬考試壓力。
- **閱讀模式**：直接瀏覽題目與答案，適合考前快速複習。
- **錯題回顧**：練習結束後自動整理答錯的題目，並可針對性檢討。

### 3. 個人化設定
- **章節篩選**：可自選特定章節進行專項練習。
- **題數調整**：支援設定 5、10、20、50 題或全部題目。
- **即時反饋**：可選擇作答後立即顯示正解，或關閉以加快練習節奏。

## 🛠️ 技術架構與檔案說明

本專案採用純前端架構，搭配 Python 腳本進行資料預處理。

- **`index.html`**：主程式，包含所有 UI 與邏輯（Tailwind CSS + Vanilla JS）。
- **`question_bank.json`**：原始題庫資料來源。
- **`process_question_bank.py`**：資料處理腳本（Python）。
  - **功能**：分析原始題庫，計算出「絕對唯一」的正確選項（排除在其他題目中作為錯誤選項出現的情況），生成白名單。
  - **輸出**：產生優化後的 `process_question_bank.json`。
- **`process_question_bank.json`**：網站實際讀取的資料檔，包含題目資訊與白名單數據。

## 📖 如何使用

### 一般使用者
1. 確保資料夾中包含 `index.html` 與 `process_question_bank.json`。
2. 雙擊 `index.html` 即可在瀏覽器中開啟使用。

### 開發者 / 維護者
若需更新題庫內容，請依照以下步驟：

1. 編輯 `question_bank.json` 修改原始題目。
2. 執行 Python 處理腳本更新資料：
   ```bash
   python process_question_bank.py
   ```
   > 此步驟會重新計算白名單邏輯並覆寫 `process_question_bank.json`。
3. 開啟 `index.html` 確認更新內容。

## 📝 題庫資料結構

原始 `question_bank.json` 格式：
```json
[
  {
    "id": 1,
    "question": "...",
    "options": {"A": "...", "B": "..."},
    "answer": "A",
    "chapter": "..."
  }
]
```

處理後 `process_question_bank.json` 格式：
```json
{
  "questions": [
    {
      "id": 1,
      "question": "...",
      "options": "...",
      "can_memorize_directly": true  // 新增欄位：是否屬於白名單題目
    }
  ],
  "answer_option_whitelist": [
    "唯一正確選項1",
    "唯一正確選項2"
  ]
}
```
